{
  launches(limit: 18, offset: 15) {
    rocket {
      rocket_name
      rocket_type
      rocket {
        id
      }
    }
    mission_name
    launch_success
    launch_date_utc
    launch_site {
      site_id
      site_name_long
      site_name
    }
    ships {
      id
    }
    mission_id
    id
  }
}
